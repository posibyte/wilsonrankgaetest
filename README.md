Wilson Rank Test for the App Engine
===================================

You can run the application with
```
dev_appserver.py wilsonrankgaetest
```
where `wilsonranktest` is the directory that contains the application.

Secret Keys can be created with
```
python secret_keys.py
```
from within the app directory.
